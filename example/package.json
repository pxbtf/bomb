{
  "name": "bomb-example",
  "version": "0.0.1",
  "description": "bomb example",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development && nodemon -e ts,ejs --exec ts-node -T ./app.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
